parent=keycloak
import=common/keycloak

styles=css/login.css

# Base URL of the Next.js app, used by template.ftl for the brand link and
# the "Créer un compte" link. Set DOCTOREK_APP_URL on the keycloak container
# in prod (falls back to localhost for local dev).
doctorekAppUrl=${env.DOCTOREK_APP_URL:http://localhost:3000}
