parent=keycloak
import=common/keycloak

styles=css/login.css

# Base URL of the Next.js app — used by template.ftl for the brand link and
# the "Créer un compte" link. Override per environment if the app moves.
doctorekAppUrl=http://localhost:3000
